// Moved to src/components/Stage.tsx (shared — project pages reuse it too).
// Re-exported here so existing homepage imports (`from "./Stage"`) keep working.
export { Stage, Place, uFont } from "@/components/Stage";
